{
  "name": "increment-kingdom",
  "version": "0.1.0",
  "private": true,
  "description": "A dependency-free idle RPG and progression simulator.",
  "scripts": {
    "build": "node scripts/build-check.js",
    "dev": "node scripts/dev-server.js"
  }
}
